# go-reloaded
## Text Modifier CLI Tool (Go) A comman-line text processing tool written in Go ## Features - Converts hexadecimal `(hex)` to decimal - Converts binary `(bin)` to decimal - Word case modifiers: - Fix punctuation spacing - Fix single quotes formatting - Convert `"a"` to `"an"` before vowels or `h` -Unit testing
```
